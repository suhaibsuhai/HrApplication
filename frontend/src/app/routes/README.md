# Routes

Routing is not installed yet. Keep module registration in `src/app/config/moduleRegistry.js` for now.
When the product needs real URLs, add React Router here and map each module to its route.
